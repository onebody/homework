# 2026-07-08 工作记录

## 暑假作业项目 Docker 化部署（进行中）
- 为 `points-system` 与 `summer-homework-checkin` 添加 Docker 部署：
  - 各项目新增 `Dockerfile`、`requirements.txt`（summer 用精简版 `requirements.docker.txt`，不含 insightface/onnxruntime/opencv —— 这三者为懒加载，仅 `/api/face` 调用时才 import，故核心打卡/积分/闯关功能不依赖它们，镜像更轻）
  - 根 `docker-compose.yml` 编排两服务：summer(8000) / points(8001)，用 named volume `summer-data`/`points-data` 持久化 SQLite 与 uploads
  - 改源码支持环境变量：`points-system/backend/app/database.py` 的 `DB_PATH`、`summer/backend/app/config.py` 的 `DB_PATH`/`UPLOAD_DIR`，实现容器内数据重定向到卷
  - 给 points-system 补了 `/api/health` 端点用于健康检查
- 修复 Docker 镜像拉取卡顿：原 `~/.docker/daemon.json` 的 3 个 registry-mirrors（ustc/tencentyun/docker-cn）全部失效（解析失败或 TCP 挂起），已清空为 `[]` 改直连 docker.io（已验证 `registry.docker.io:443` 直连可用）
- **阻塞点**：清空 mirrors 需重启 Docker daemon 加载，但自动化环境无法拉起 Docker Desktop（卡 GUI 授权/无桌面会话），当前 Docker 已停止。需用户在 macOS 手动启动 Docker Desktop 后，再执行 `cd /Users/fcj/workspace/hanghang_WS && docker compose up -d --build` 完成构建与部署。

## ✅ Docker 部署成功（用户手动启动 Docker 后完成）
- 用户在 macOS 手动启动 Docker Desktop，构建与部署一次成功。
- **关键网络根因**：之前卡死不是镜像源配置问题，而是**当前网络无法访问 Docker Hub 的 HTTPS 层**（TCP 443 能连，但 TLS 握手/HTTP 请求 24s 超时）。直连 docker.io 拉不到镜像。
  - 探测可用国内源：`docker.m.daocloud.io`（401=可达，是 Docker Hub 代理）、`docker.1panel.live`（200）、`mirrors.aliyun.com/pypi`（pip 源，170ms 最快）。
- **零联网构建方案**：本地已有镜像 `public.ecr.aws/docker/library/python:3.11-slim`（214MB），直接 `docker tag` 为 `docker.m.daocloud.io/library/python:3.11-slim`，Dockerfile `FROM` 用该前缀 → build 命中本地镜像、不联网。pip 源改用阿里云 `mirrors.aliyun.com/pypi/simple`（170ms）。
- **验证全部通过**：
  - summer-homework 容器 `healthy`，端口 8000；学生端 `/`、管理端 `/admin/` 均 200；admin/admin123 登录 OK；4 个闯关任务 seed 正确。
  - points-system 容器 `healthy`，端口 8001；前端 `/` 200；奖品4/奖池12/用户2（xiaoming,xiaohong）数据正常；`/api/dashboard?user_id=1` 返回看板（余额260）。points 为**免密码 username 登录**（前端下拉选用户；其 router prefix=/api，故 dashboard 路径是 `/api/dashboard` 而非 `/api/users/dashboard`）。
- 数据卷 `summer-data`/`points-data` 持久化 SQLite 与 uploads；两容器 `restart: unless-stopped`。
- 常用命令：`docker compose ps` / `docker compose logs -f` / `docker compose down`（加 `-v` 删数据）。

## ✅ 管理后台全面适配手机端（响应式单代码库）
- 需求：整个管理后台要能在手机上直接访问管理，PC 端也正常。采用**响应式增强**（非独立页面），一套代码双端适配。
- 改动文件均位于 `summer-homework-checkin/frontend/admin/`：`index.html` / `app.js` / `admin.css`。
- 之前已有基础响应式：侧栏→底部 Tab、兑换表→卡片、弹窗→底部抽屉、看图器已适配。本次补齐：
  - 为**奖品/用户/打卡/闯关任务/闯关打卡** 5 张表各新增手机卡片视图（`.prize-cards` 等），复用现有 JS 方法（openEdit/del/openReview/viewCheckinPhoto/openChallengeForm/unlockTask/viewChallengeCheckins/deleteTask/openChallengeReview/viewChallengeAttachments 均已存在）。
  - 新增**手机端顶部栏** `.m-topbar`：显示当前模块中文名（computed `currentViewName`）+ 退出登录按钮（原来手机端 `.logout` 被隐藏导致无法退出，已修复）。
  - app.js 新增 `thumbUrl(url)` 方法（模板中无法调用全局 `fixUrl`，故暴露为实例方法用于卡片缩略图地址补全）。
  - 响应式断点由 640px 统一改为 **768px**；手机端：侧栏→底部 Tab（含 iPhone 安全区 `env(safe-area-inset-*)`）、表格隐藏+卡片显示、弹窗变底部全屏抽屉、触控点击区≥44px、登录页宽度 90%。
  - PC 端（>768px）：`.m-topbar` 隐藏、卡片容器 `display:none`、表格照常显示 → 布局完全不变。
- 验证：重建 summer 容器（pip 层缓存，仅重拷 frontend，秒级），`/admin/` 返回 200，部署后页面与 admin.css 均含新卡片/顶栏标记，CSS 括号平衡（206/206）。
- 已同步到 `homework-monorepo` 并提交 push 到 GitHub `onebody/homework`（commit 7677ad5）。
